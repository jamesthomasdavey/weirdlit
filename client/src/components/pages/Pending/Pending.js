import React, { Fragment } from 'react';
import Navbar from './../../layout/Navbar/Navbar';

const Pending = () => {
  return (
    <Fragment>
      <Navbar />
    </Fragment>
  );
};

export default Pending;
